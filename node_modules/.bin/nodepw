#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../password-hash/bin/nodepw" "$@"
  ret=$?
else 
  node  "$basedir/../password-hash/bin/nodepw" "$@"
  ret=$?
fi
exit $ret
